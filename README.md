# Connect 4

This is a game of connect 4 made entirely in C that runs in a terminal.

## How to build and run
### (Mac OS / Linux)
(Make sure you have GCC installed) \
\
./build.sh \
./run.sh

### (Windows)
(Make sure you have Mysys2 installed) \
\
build.bat \
run.bat
